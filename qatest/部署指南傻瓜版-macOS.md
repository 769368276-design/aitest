# 本地安装快速指南（macOS）

本包给客户使用时，仅考虑**本地安装与本地访问**（localhost）。不涉及服务器部署。

请以根目录的「本地安装说明.md」为准：里面包含 Windows/macOS 的一键安装、离线 wheelhouse、代理/网络、Playwright 等完整排查方案。

## 最快安装
1）进入项目根目录（能看到 `manage.py`、`requirements.txt`、`安装点我.command`）  
2）双击 `安装点我.command`  
3）浏览器打开：`http://localhost:8003/`
